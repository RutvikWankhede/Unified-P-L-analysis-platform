{
  "AllowMultiple": false,
  "Description": "Refactor pl_router.py charts logic to use MetricEngine.aggregate_data",
  "EndLine": 668,
  "Instruction": "Replace manual SQLite queries with MetricEngine pandas aggregation in _fetch_charts",
  "ReplacementContent": "            from services.metric_engine import MetricEngine\n            me = MetricEngine(db, active_id)\n            caps = me.get_capabilities()\n            profile = me.get_active_profile()\n\n            df_agg = me.aggregate_data(resolved_agg, dept)\n            \n            revenue_trend = []\n            expense_trend = []\n            profit_trend = []\n            cashflow_trend = []\n            \n            if not df_agg.empty:\n                periods = df_agg[\"period\"].tolist()\n                \n                if caps[\"revenue\"][\"available\"]:\n                    revenue_trend = [{\"period\": r[\"period\"], \"value\": round(r[\"revenue\"])} for _, r in df_agg.iterrows()]\n                if caps[\"expense\"][\"available\"]:\n                    expense_trend = [{\"period\": r[\"period\"], \"value\": round(r[\"expense\"])} for _, r in df_agg.iterrows()]\n                if caps[\"profit\"][\"available\"]:\n                    profit_trend = [{\"period\": r[\"period\"], \"value\": round(r[\"profit\"])} for _, r in df_agg.iterrows()]\n                \n                if profile[\"capabilities\"][\"cash_flow\"]:\n                    cashflow_trend = []\n                    for _, r in df_agg.iterrows():\n                        val = r[\"net_cash_flow\"]\n                        cashflow_trend.append({\"period\": r[\"period\"], \"value\": round(val) if val is not None else None})\n            else:\n                periods = []\n                \n            all_depts = profile[\"dimension_values\"]\n            \n            dept_breakdown = []\n            dept_aggregates = me.get_department_aggregates()\n            for d in dept_aggregates:\n                dept_breakdown.append({\n                    \"department\": d[\"department\"],\n                    \"revenue\": round(d[\"revenue\"]) if d[\"revenue\"] is not None else None,\n                    \"expense\": round(d[\"expense\"]) if d[\"expense\"] is not None else None,\n                    \"profit\": round(d[\"profit\"]) if d[\"profit\"] is not None else None,\n                })\n                \n            total_rev = sum(d[\"revenue\"] or 0 for d in dept_aggregates)\n            total_exp = sum(d[\"expense\"] or 0 for d in dept_aggregates)\n            \n            waterfall = []\n            if caps[\"revenue\"][\"available\"]:\n                waterfall.append({\"name\": \"Revenue\", \"value\": round(total_rev), \"type\": \"revenue\"})\n            if caps[\"expense\"][\"available\"]:\n                waterfall.append({\"name\": \"Expenses\", \"value\": round(total_exp), \"type\": \"expense\"})\n            if caps[\"profit\"][\"available\"]:\n                waterfall.append({\n                    \"name\": \"Net Profit\",\n                    \"value\": round(total_rev - total_exp),\n                    \"type\": \"profit\",\n                })\n                \n            scatter_query = db.query(PLRecord)\n            if active_id:\n                scatter_query = scatter_query.filter(PLRecord.upload_id == active_id)\n            scatter_records = scatter_query.limit(200).all()\n            scatter = [\n                {\"x\": i, \"y\": round(r.amount), \"domain\": r.domain, \"line_item\": r.line_item}\n                for i, r in enumerate(scatter_records)\n            ]\n            \n            df_dim = me.aggregate_by_dimension(resolved_agg)\n            \n            dept_profit_trend = []\n            heatmap_data_dict = {}\n            if not df_dim.empty:\n                for _, r in df_dim.iterrows():\n                    dept_profit_trend.append({\n                        \"period\": r[\"group_period\"],\n                        \"department\": r[\"domain\"],\n                        \"profit\": round(r[\"profit\"])\n                    })\n                    heatmap_data_dict[(r[\"group_period\"], r[\"domain\"])] = round(r[\"amount_exp\"])\n                    \n            heatmap_data = []\n            for (p, d), val in heatmap_data_dict.items():\n                try:\n                    p_idx = periods.index(p)\n                    d_idx = all_depts.index(d)\n                    heatmap_data.append([p_idx, d_idx, round(val)])\n                except ValueError:\n                    pass\n\n            res = {\n                \"revenue_trend\": revenue_trend,\n                \"expense_trend\": expense_trend,\n                \"profit_trend\": profit_trend,\n                \"cashflow_trend\": cashflow_trend,\n                \"department_breakdown\": dept_breakdown,\n                \"waterfall\": waterfall,\n                \"scatter\": scatter,\n                \"heatmap\": heatmap_data,\n                \"dept_profit_trend\": dept_profit_trend,\n                \"periods\": periods,\n                \"departments\": all_depts,\n            }\n            set_cached_item(cache_key, res)\n            return res\n        except Exception as e:\n            import logging\n            logging.getLogger(__name__).error(f\"Charts endpoint error: {e}\")\n            return {\n                \"revenue_trend\": [],\n                \"expense_trend\": [],\n                \"profit_trend\": [],\n                \"department_breakdown\": [],\n                \"waterfall\": [],\n                \"scatter\": [],\n            }",
  "StartLine": 464,
  "TargetContent": "            is_rev = or_(\n                PLRecord.line_item.ilike(\"%revenue%\"),\n                PLRecord.line_item.ilike(\"%income%\"),\n                PLRecord.line_item.ilike(\"%sales%\")\n            )\n            is_exp = or_(\n                PLRecord.line_item.ilike(\"%expense%\"),\n                PLRecord.line_item.ilike(\"%cost%\"),\n                PLRecord.line_item.ilike(\"%cogs%\"),\n                PLRecord.line_item.ilike(\"%opex%\"),\n                PLRecord.line_item.ilike(\"%salary%\"),\n                PLRecord.line_item.ilike(\"%salaries%\"),\n                PLRecord.line_item.ilike(\"%fees%\"),\n                PLRecord.line_item.ilike(\"%maintenance%\"),\n                PLRecord.line_item.ilike(\"%logistics%\"),\n                PLRecord.line_item.ilike(\"%facilities%\"),\n                PLRecord.line_item.ilike(\"%commission%\"),\n                PLRecord.line_item.ilike(\"%advertising%\"),\n                PLRecord.line_item.ilike(\"%recruitment%\"),\n                PLRecord.line_item.ilike(\"%training%\"),\n                PLRecord.line_item.ilike(\"%benefits%\"),\n                PLRecord.line_item.ilike(\"%audit%\"),\n                PLRecord.line_item.ilike(\"%compliance%\"),\n                PLRecord.line_item.ilike(\"%banking%\"),\n                PLRecord.line_item.ilike(\"%events%\"),\n                PLRecord.line_item.ilike(\"%agency%\"),\n                PLRecord.line_item.ilike(\"%licenses%\"),\n                PLRecord.line_item.ilike(\"%r&d%\"),\n                PLRecord.line_item.ilike(\"%crm%\"),\n                PLRecord.line_item.ilike(\"%travel%\")\n            )\n\n            category = case(\n                (is_rev, \"revenue\"),\n                (is_exp, \"expense\"),\n                else_=\"other\"\n            )\n\n            if resolved_agg == \"daily\":\n                period_expr = PLRecord.period\n            elif resolved_agg == \"weekly\":\n                period_expr = func.strftime(\"%Y-W%W\", PLRecord.period)\n            elif resolved_agg == \"quarterly\":\n                period_expr = func.substr(PLRecord.period, 1, 4) + \"-Q\" + case(\n                    (func.substr(PLRecord.period, 6, 2).in_([\"01\", \"02\", \"03\"]), \"1\"),\n                    (func.substr(PLRecord.period, 6, 2).in_([\"04\", \"05\", \"06\"]), \"2\"),\n                    (func.substr(PLRecord.period, 6, 2).in_([\"07\", \"08\", \"09\"]), \"3\"),\n                    else_=\"4\"\n                )\n            elif resolved_agg == \"half-yearly\":\n                period_expr = func.substr(PLRecord.period, 1, 4) + \"-H\" + case(\n                    (func.substr(PLRecord.period, 6, 2).in_([\"01\", \"02\", \"03\", \"04\", \"05\", \"06\"]), \"1\"),\n                    else_=\"2\"\n                )\n            elif resolved_agg == \"yearly\":\n                period_expr = func.substr(PLRecord.period, 1, 4)\n            else: # monthly\n                period_expr = func.substr(PLRecord.period, 1, 7)\n\n            query = db.query(\n                PLRecord.domain,\n                period_expr.label(\"period_str\"),\n                category.label(\"category\"),\n                func.sum(PLRecord.amount).label(\"total_amount\")\n            )\n\n            if active_id:\n                query = query.filter(PLRecord.upload_id == active_id)\n            if dept and dept.lower() != \"all\" and dept != \"All Departments\":\n                query = query.filter(PLRecord.domain == dept)\n\n            query = query.group_by(PLRecord.domain, period_expr, category)\n            results = query.all()\n\n            period_rev = defaultdict(float)\n            period_exp = defaultdict(float)\n            dept_rev = defaultdict(float)\n            dept_exp = defaultdict(float)\n            heatmap_data_dict = defaultdict(float)\n            dept_profit_dict = defaultdict(float)\n\n            for domain, period_str, cat, amount in results:\n                amount = float(amount or 0.0)\n                if cat == \"revenue\":\n                    period_rev[period_str] += amount\n                    dept_rev[domain] += amount\n                    dept_profit_dict[(period_str, domain)] += amount\n                elif cat == \"expense\":\n                    period_exp[period_str] += amount\n                    dept_exp[domain] += amount\n                    dept_profit_dict[(period_str, domain)] -= amount\n                    heatmap_data_dict[(period_str, domain)] += amount\n\n            periods = sorted(set(list(period_rev.keys()) + list(period_exp.keys())))\n            all_depts = sorted(set(list(dept_rev.keys()) + list(dept_exp.keys())))\n\n            from services.metric_engine import MetricEngine\n            me = MetricEngine(db, active_id)\n            caps = me.get_capabilities()\n\n            revenue_trend = []\n            if caps[\"revenue\"][\"available\"]:\n                revenue_trend = [{\"period\": p, \"value\": round(period_rev[p])} for p in periods]\n                \n            expense_trend = []\n            if caps[\"expense\"][\"available\"]:\n                expense_trend = [{\"period\": p, \"value\": round(period_exp[p])} for p in periods]\n                \n            profit_trend = []\n            if caps[\"profit\"][\"available\"]:\n                profit_trend = [\n                    {\"period\": p, \"value\": round(period_rev[p] - period_exp[p])}\n                    for p in periods\n                ]\n                \n            cashflow_trend = []\n            if caps[\"cashFlow\"][\"available\"]:\n                cashflow_trend = [\n                    {\"period\": p, \"value\": round((period_rev[p] - period_exp[p]) * 1.15)}\n                    for p in periods\n                ]\n\n            dept_breakdown = []\n            for d in all_depts:\n                dept_breakdown.append(\n                    {\n                        \"department\": d,\n                        \"revenue\": round(dept_rev[d]) if caps[\"revenue\"][\"available\"] else None,\n                        \"expense\": round(dept_exp[d]) if caps[\"expense\"][\"available\"] else None,\n                        \"profit\": round(dept_rev[d] - dept_exp[d]) if caps[\"profit\"][\"available\"] else None,\n                    }\n                )\n\n            total_rev = sum(period_rev.values()) if caps[\"revenue\"][\"available\"] else 0\n            total_exp = sum(period_exp.values()) if caps[\"expense\"][\"available\"] else 0\n            \n            waterfall = []\n            if caps[\"revenue\"][\"available\"]:\n                waterfall.append({\"name\": \"Revenue\", \"value\": round(total_rev), \"type\": \"revenue\"})\n            if caps[\"expense\"][\"available\"]:\n                waterfall.append({\"name\": \"Expenses\", \"value\": round(total_exp), \"type\": \"expense\"})\n            if caps[\"profit\"][\"available\"]:\n                waterfall.append({\n                    \"name\": \"Net Profit\",\n                    \"value\": round(total_rev - total_exp),\n                    \"type\": \"profit\",\n                })\n\n            scatter_query = db.query(PLRecord)\n            if active_id:\n                scatter_query = scatter_query.filter(PLRecord.upload_id == active_id)\n            scatter_records = scatter_query.limit(200).all()\n            scatter = [\n                {\"x\": i, \"y\": round(r.amount), \"domain\": r.domain, \"line_item\": r.line_item}\n                for i, r in enumerate(scatter_records)\n            ]\n\n            dept_profit_trend = []\n            for (p, d), val in dept_profit_dict.items():\n                dept_profit_trend.append({\n                    \"period\": p,\n                    \"department\": d,\n                    \"profit\": round(val)\n                })\n\n            heatmap_data = []\n            for (p, d), val in heatmap_data_dict.items():\n                try:\n                    p_idx = periods.index(p)\n                    d_idx = all_depts.index(d)\n                    heatmap_data.append([p_idx, d_idx, round(val)])\n                except ValueError:\n                    pass\n\n            res = {\n                \"revenue_trend\": revenue_trend,\n                \"expense_trend\": expense_trend,\n                \"profit_trend\": profit_trend,\n                \"cashflow_trend\": cashflow_trend,\n                \"department_breakdown\": dept_breakdown,\n                \"waterfall\": waterfall,\n                \"scatter\": scatter,\n                \"heatmap\": heatmap_data,\n                \"dept_profit_trend\": dept_profit_trend,\n                \"periods\": periods,\n                \"departments\": all_depts,\n            }\n            set_cached_item(cache_key, res)\n            return res\n        except Exception as e:\n            import logging\n            logging.getLogger(__name__).error(f\"Charts endpoint error: {e}\")\n            return {\n                \"revenue_trend\": [],\n                \"expense_trend\": [],\n                \"profit_trend\": [],\n                \"department_breakdown\": [],\n                \"waterfall\": [],\n                \"scatter\": [],\n            }",
  "TargetFile": "c:\\Users\\HP\\.gemini\\antigravity-ide\\scratch\\P&L system\\unified-pl-system\\backend\\routers\\pl_router.py"
}